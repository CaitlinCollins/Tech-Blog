<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<title>Tech Today</title>
	<link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div id="container">
        <header>
            <div id="navbar">
                <a href="/home">Home</a>
                <a href="/dashboard">Dashboard</a>
                {{#unless loggedIn}}
                <a href="/">Login</a>
                {{/unless}}
                {{#if loggedIn}}
                <a id="logout" href="/">Logout</a>
                {{/if}}
            </div>
            <div>
                <h1>Tech Today Blog</h1>
            </div>
        </header>
        <main>
            {{{ body }}}
        </main>
   </div>
    <footer id="lowerContainer" style="text-align:center">
        <div id="footer">&copy;2021 <a href="/">Tech Today</a>
        </div>
    </footer>
    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>